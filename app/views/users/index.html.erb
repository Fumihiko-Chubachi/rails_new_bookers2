<% @users.each do |user| %>
<div>
    <%= image_tag user.get_profile_image(100,100) %><br>
  <p><%= user.name %></p>
  <%= link_to 'Show', user_path(user.id) %><!--他ユーザーのプロフィール編集を出来ないようにする制御必要-->
</div>
<% end %>